class Solution:
    def sortBinaryArray (self, arr, n):
        a=[]
        for i in range(arr.count(0)):
            a.append(0)
        for j in range(arr.count(1)):
            a.append(1)
        return a 
        
