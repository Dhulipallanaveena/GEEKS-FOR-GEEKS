class Solution:
    def minDist(self, arr, n, x, y):
        for i in range(n):
            for j in range(i+1,n):
                if x==arr[i] and  y==arr[j] or x==arr[j] and y==arr[i]:
                    return abs(i-j)
        
        
    
